---
"@vui-rs/core": minor
"@vui-rs/vue": minor
---

Detect and follow the terminal's light/dark theme: OSC 11 background query
(`queryColorScheme`), DEC mode 2031 change notifications decoded as `ThemeEvent`,
and an `onThemeChange` host mount hook.
