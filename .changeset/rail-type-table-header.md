---
"@vui-rs/vue": minor
"@vui-rs/ui": minor
---

expose `rail`/`railColor` on the `<box>` element type (the open left guide-rail paint primitive) and add a `header` prop to `VuiTable` for a headerless (bare cell-grid) mode
