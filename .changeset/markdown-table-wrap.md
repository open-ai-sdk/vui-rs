---
"@vui-rs/vue": minor
---

render markdown tables inside a rounded border with word-wrapping cells: column widths fit the table's measured width and long cells wrap to multiple lines (preserving inline bold/code styling) instead of overflowing off-screen
